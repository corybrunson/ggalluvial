% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uncross.r
\name{register_uncross_engine}
\alias{register_uncross_engine}
\alias{uncross_engines}
\title{Register an uncrossing engine}
\usage{
register_uncross_engine(
  name,
  sort_strata = NULL,
  sort_axes = NULL,
  color_strata = NULL
)

uncross_engines()
}
\arguments{
\item{name}{Character; the name under which to register the engine.}

\item{sort_strata, sort_axes, color_strata}{Functions implementing the
contract above, or \code{NULL} if the engine does not provide that operation.}
}
\value{
Invisibly, the registered engine, as a list.

\code{uncross_engines()} returns the names of the registered engines.
}
\description{
Makes an algorithm for ordering or coloring strata available to the
\code{sort_strata} and \code{color_strata} parameters of \code{\link[=stat_stratum]{stat_stratum()}},
\code{\link[=stat_alluvium]{stat_alluvium()}}, and \code{\link[=stat_flow]{stat_flow()}}, and to \code{\link[=sort_axes]{sort_axes()}}, under a name
that those parameters will recognize.
}
\details{
Each of \code{sort_strata}, \code{sort_axes}, and \code{color_strata} is a function of four
arguments:

\describe{
\item{\code{wide}}{a data frame in alluvia form: one row per alluvium, one
character column per axis (named by \code{axes}), and one numeric column
(named by \code{weight}) holding the size of the alluvium.}
\item{\code{axes}}{a character vector of the axis column names of \code{wide}, in
plot order.}
\item{\code{weight}}{the name of the weight column of \code{wide}.}
\item{\code{method}}{the algorithm to use, or \code{NULL} for the engine's own
default.}
}

plus any additional arguments the caller passed alongside \code{method}. They
must return, respectively:

\describe{
\item{\code{sort_strata}}{a list, named by \code{axes}, of character vectors giving
the strata at that axis in ascending order.}
\item{\code{sort_axes}}{a character vector: a permutation of \code{axes}.}
\item{\code{color_strata}}{a list, named by \code{axes}, of named vectors mapping
each stratum at that axis to a cluster identifier.}
}
}
\seealso{
\link{uncross} for how engines are chosen.
}
